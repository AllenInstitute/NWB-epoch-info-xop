Xcode creates the libXOPSupport64.a file in the build/Release directory
(if you build the release configuration).

After rebuilding the library, you must move the libXOPSupport64.a file from the
Release directory to the Xcode directory.
